# Hacs

Hacs是一个使用使用鼠标实现快捷键操作的工具。

Hacs的名字是“Hexagon-alt-ctrl-shift”的简写，分别对应以六边形为主的UI和三个修饰键。

## 使用

使用Hacs发送一个快捷键的流程如下（以QQ截图快捷键Ctrl+Alt+A为例）：

1. 启动Hacs；
2. 在你需要截图的界面按下鼠标右键并向下拖动，此时屏幕上会出现一个底边与x轴平行、顶角朝上的蓝绿色的半透明整三角形，然后松开鼠标右键；
3. 松开鼠标右键，此时屏幕上会出现若干个蓝绿色半透明六边形，在其中找到带有镂空文字为“A”的六边形，用鼠标左键单击即可完成快捷键操作。

## 配置

### 按键内容

通过家界面的“Add”按钮或者“Modify”按钮更新快捷键内容时，请遵循以下规则。

1. 大写字母表示对应的字母按键，如“A”表示按下键盘上的A键；
2. 数字和符号直接表示主键盘区上对应的按键，以非上档模式出现的字符为准，以标准配列为例，“`1234567890-=”表示顺次按下字母区上方的反引号、数字和减号和等于号；
3. 对其它的按键，请使用“{按键名}”的形式，对部分常用按键提供了单个小写字母的形式，如“{Enter}”和“e”都表示回车键。具体请参阅下表：


| 按键    | 文本                                                                                                   | 备注                             |
| ------- | ------------------------------------------------------------------------------------------------------ | -------------------------------- |
| 回车    | "e" <br> "{ENTER}" <br> "{enter}" <br> "{Enter}"                                                       | 不保证能触发小键盘区的回车       |
| 制表    | "t" <br> "{TAB}" <br> "{tab}" <br> "{Tab}"                                                             |                                  |
| 退格    | "b" <br> "{BACKSPACE}" <br> "{backspace}" <br> "{Backspace}" <br> "{BACK}" <br> "{back}" <br> "{Back}" |                                  |
| 空格    | "s" <br> "{SPACE}" <br> "{space}" <br> "{Space}"                                                       |                                  |
| 删除    | "d" <br> "{DELETE}" <br> "{delete}" <br> "{Delete}"                                                    | 不保证等同于小键盘上的.          |
| 插入    | "i" <br> "{INSERT}" <br> "{insert}" <br> "{Insert}"                                                    | 不保证等同于小键盘上的0          |
| 首页    | "h" <br> "{HOME}" <br> "{home}" <br> "{Home}"                                                          | 不保证等同于小键盘上的7          |
| 结束    | "{End}" <br> "{end}" <br> "{END}"                                                                      | 不保证等同于小键盘上的1          |
| 上翻页  | "{PAGEUP}" <br> "{pageup}" <br> "{Pageup}" <br> "{PgUp}" <br> "{pgup}" <br> "{Pgup}"                   | 不保证等同于小键盘上的9          |
| 下翻页  | "{PAGEDOWN}" <br> "{pagedown}" <br> "{Pagedown}" <br> "{PgDn}" <br> "{pgdn}" <br> "{Pgdn}"             | 不保证等同于小键盘上的3          |
| 退出    | "{Esc}" <br> "{ESC}" <br> "{esc}"                                                                      |                                  |
| 小键盘0 | "{num0}" <br> "{Num0}" <br> "{NUM0}"                                                                   | 特指小键盘上的按键，下面几个同理 |
| 小键盘1 | "{num1}" <br> "{Num1}" <br> "{NUM1}"                                                                   |                                  |
| 小键盘2 | "{num2}" <br> "{Num2}" <br> "{NUM2}"                                                                   |                                  |
| 小键盘3 | "{num3}" <br> "{Num3}" <br> "{NUM3}"                                                                   |                                  |
| 小键盘4 | "{num4}" <br> "{Num4}" <br> "{NUM4}"                                                                   |                                  |
| 小键盘5 | "{num5}" <br> "{Num5}" <br> "{NUM5}"                                                                   |                                  |
| 小键盘6 | "{num6}" <br> "{Num6}" <br> "{NUM6}"                                                                   |                                  |
| 小键盘7 | "{num7}" <br> "{Num7}" <br> "{NUM7}"                                                                   |                                  |
| 小键盘8 | "{num8}" <br> "{Num8}" <br> "{NUM8}"                                                                   |                                  |
| 小键盘9 | "{num9}" <br> "{Num9}" <br> "{NUM9}"                                                                   |                                  |
| F1      | "{F1}" <br> "{f1}"                                                                                     |                                  |
| F2      | "{F2}" <br> "{f2}"                                                                                     |                                  |
| F3      | "{F3}" <br> "{f3}"                                                                                     |                                  |
| F4      | "{F4}" <br> "{f4}"                                                                                     |                                  |
| F5      | "{F5}" <br> "{f5}"                                                                                     |                                  |
| F6      | "{F6}" <br> "{f6}"                                                                                     |                                  |
| F7      | "{F7}" <br> "{f7}"                                                                                     |                                  |
| F8      | "{F8}" <br> "{f8}"                                                                                     |                                  |
| F9      | "{F9}" <br> "{f9}"                                                                                     |                                  |
| F10     | "{F10}" <br> "{f10}"                                                                                   |                                  |
| F11     | "{F11}" <br> "{f11}"                                                                                   |                                  |
| F12     | "{F12}" <br> "{f12}"                                                                                   |                                  |


### 选择面板

选择面板指的是当你松开鼠标右键后弹出的选择快捷键的界面。

选择面板的大小是1000*1000，以左下为x、y轴的正方向。当你编辑某个快捷键按钮的OffsetX或者OffsetY时，指的是按钮的中心相对于选择面板的中心的偏移量。其中，按钮是高100、总长200、上下边长100的六边形。(0,0)的位置已被一个对角线长为50的菱形取消按钮所占据。

## 功能

### 开机自启动

还未实现。

### 常驻托盘

在启动Hacs后能在右下角的系统托盘看到一个Hacs的Logo。单击这个图标就能打开Home窗口，右键这个图标能打开一个菜单。

### 家窗口

可以在这里配置快捷键。

### 日志窗口 Logs

通过系统托盘图标的右键菜单进入。

### 多显示器支持

几乎没做。

## 饼

- [x] 解决出现KeysForm后会导致鼠标卡顿的问题
- [x] 实现发送快捷键的功能
- [x] 实现Home界面，用户可以在这里配置快捷键
- [x] 在托盘图标的菜单中添加一个开关

以上是1.0版本以前的。

- [ ] 调整日志模块 
- [ ] 开机自启动
- [ ] 多显示器适配
- [ ] 多语言界面
- [ ] 更好的文档
- [ ] 支持更多的按键

别画了，吃不下了……